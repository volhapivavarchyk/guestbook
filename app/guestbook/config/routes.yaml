indexRoute:
    path:     /
    defaults: { controller: 'Piv\Guestbook\Src\Controllers\UserController::show' }

sortingRoute:
    path:     /{sortflag}/{count}
    defaults: { controller: 'Piv\Guestbook\Src\Controllers\UserController::show' }
    requirements:
      sortflag: '(ByName|ByEmail|ByDate)(Asc|Desc)'
      count:    \d+
